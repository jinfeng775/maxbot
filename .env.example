# MaxBot 环境变量

# OpenAI
OPENAI_API_KEY=sk-...

# 或用兼容接口（如 DeepSeek、Ollama、本地部署）
MAXBOT_BASE_URL=http://localhost:11434/v1
MAXBOT_MODEL=qwen2.5

# Brave Search（可选，不设则回退到 DuckDuckGo）
# BRAVE_API_KEY=...

# 自定义数据目录（可选，默认 ~/.maxbot）
# MAXBOT_HOME=~/.maxbot
